<%- item = assigns['node'] -%>
<% content_tag_for :li, item, 'item_for', :class => dom_class(item, 'item_for') do -%>
	<div class="body">
		<%= render :partial => 'nodes/action_buttons', :locals => {:item => item} %>
		<%= @content_for_layout %>
	</div>
	<%= render :partial => 'nodes/item_list', :locals => {:item => item} %>
<% end -%>
