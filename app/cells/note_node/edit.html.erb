<div class="edit note prop shadowable">
	<div class="bordered shadow"></div>
	<% remote_form_for :prop, @prop, :url => node_path(@node), :html => { :method => :put, :class => dom_class(@prop, 'edit'), :id => dom_id(@prop, 'edit') } do |f| %>
		<%= f.text_area :note, :onblur => "$(this).next('input[type=submit]').click();" %>
		<%# ✓ %><%= f.submit '&#10003;', :style => 'display: none;' %>
	<% end %>
	<script>$('#<%=dom_id(@prop, 'edit')%>').children('textarea:first').focus();</script>
</div>
