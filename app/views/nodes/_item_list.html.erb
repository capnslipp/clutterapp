<ul class="node list">
	<%- item.children.each do |child| -%>
		<%= render_child(child) %>
	<%- end unless item.children.empty? -%>
	<%= render :partial => 'nodes/new_buttons', :locals => {:item => item} %>
</ul>
