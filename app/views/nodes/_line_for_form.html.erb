<div class="line">
	<%= form.hidden_field(:_destroy) %>
	<a href="#" class="remove button" onclick="return false;"><span class="sym"><%# ✖ %>&#10006;</span></a>
	<em><%= node.prop.class.short_name %></em>
	<%= render :partial => prop_partial(node, state), :locals => { :node => node, :form => form } %>
</div>
