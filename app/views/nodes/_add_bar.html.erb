<li id="add-bar" class="add bar">
	
	<%- Prop.badgeable_types.each do |t| -%>
		<%- next unless t.stackable? || !node.children.find(:first, :conditions => {:prop_type => t.to_s}) -%>
		<%- ability_classes = Prop.abilities.select{|a| t.send("#{a}?") }.collect{|a| a.to_s }.join(' ') -%>
		<a href="#" class="add <%= t.short_name %> <%= ability_classes %> button">
			<span class="sym"><%# ✚ %>&#10010;</span> <%= t.short_name %>
		</a>
	<%- end -%>
	
</li>
