/* item list (old) */

ul.list ul.list {
	border-left: 1px dotted <%= divider_color * 0.5 + "#fff".to_color * 0.5 %>;
	padding-left: 18px;/*
	/*margin-left: -1em;*/
}

li { list-style-type: none; }

.pile.item_for_node > .body { display: block; margin-bottom: 18px; margin-left: 0; border: none; }
.pile.item_for_node > .body > .header {}
.pile.item_for_node > .body > .header > .owner { opacity: 0.25; }
.pile.item_for_node > .body > .header > .owner,
.pile.item_for_node > .body > .header > .name { white-space: nowrap; }
.pile.item_for_node > .body > .action.stub { float: right; width: auto; border: none; }

.item_for_node {
	position: relative;
	
	<% if debug? %>
		background: rgba(255, 127, 0, 0.125);
		border: 2px solid rgba(255, 127, 0, 0.5);
		<%= border_radius_decl %>
		margin: 1px;
	<% end %>
}
.item_for_node > .body {
	<% if debug? %>
		<%= border_radius_decl %>
		margin: 1px;
	<% end %>
	
	/*min-height: 1.5em;*/
	border-top: 1px dotted <%= divider_color %>;
	padding: 2px 0;
	padding-left: 24px; /* for "> .bullet" */
}
.item_for_node > .show.body {
	<% if debug? %>
		background: rgba(127, 191, 0, 0.125);
		border: 2px solid rgba(127, 191, 0, 0.5);
	<% end %>
}
.item_for_node > .show.body.active {
	background: <%= bright_bg_color %>;
	<%= deep_shadow_decl(shadow_color.with(:a =>0.1)) %>
}
.item_for_node > .show.body > .cont {
}
.item_for_node > .new.body,
.item_for_node > .edit.body {
	margin: -12px 8px;
	
	background: <%= accent_color.with(:a => 0.5).to_s(:css_rgba) %>;
	<%= border_radius_decl('16px') %>
	border: 8px solid transparent;
}
.item_for_node > .new.body > .cont,  .item_for_node > .new.body > form > .cont,
.item_for_node > .edit.body > .cont, .item_for_node > .edit.body > form > .cont {
	<%= border_radius_decl('8px') %>
	<%= shallow_shadow_decl %>
	
	background: <%= bright_bg_color %>;
	padding: 8px;
}
.item_for_node > .edit.body {
	position: absolute;
	/*width: 100%;*/ left: 0; right: 0;
	
	<% if debug? %>
		background: rgba(191, 0, 127, 0.125);
		border: 2px solid rgba(191, 0, 127, 0.5);
	<% end %>
}
.item_for_node > .new.body {
	position: relative;
	
	<% if debug? %>
		background: rgba(0, 255, 191, 0.125);
		border: 2px solid rgba(0, 255, 191, 0.5);
	<% end %>
}
.new.item_for_node {
	position: absolute;
	/*width: 100%;*/ left: 0; right: 0;
}
.item_for_node > .body .line {
	/*overflow: auto; width: 100%; /* to contain floating elements */
	margin-top: 2px;
	margin-bottom: 4px;
}
.item_for_node > .body > .bullet {
	float: left;
	margin-left: -20px;
	width: 16px;
	margin-right: 4px;
	text-align: center;
	vertical-align: middle;
}
.item_for_node > .body > .bullet .sym { font-size: 2em; line-height: 1em; }

.item_for_node > .body > .action.stub,
.item_for_node > .body > #action-bar {
	float: right;
	margin-right: 4px;
}

/* page */
#page {
	padding: 16px;
}


/* top-bar */

#top-bar {
	position: relative; z-index: 200;
}

#top-bar, #top-bar a {
	color: <%= wash_text_color %>;
}

/* contain-float-after */
#top-bar:after { content: ""; display: block; height: 0; clear: both; visibility: hidden; /* (FF, S2, O9) */ }

<%- top_bar_back_shadow_height = 10 -%>

#top-bar > .back {
	overflow: hidden;
	
	position: absolute;
	width: 100%; height: 100%;
	
	padding-bottom: <%=top_bar_back_shadow_height%>px;
}

#top-bar > .back > .fill {
	position: absolute;
	z-index: -1;
	top: 0; bottom: <%=top_bar_back_shadow_height%>px;
	top: -26px; left: -26px; right: -26px;
	
	background: <%= wash_color %>;
	
	margin-bottom: -<%= border_width %>px;
	border-bottom: <%= border_width %>px solid <%= generic_border_color.to_s(:css_rgba) %>;
	
	<%= shallow_shadow_decl %>
}

#top-bar > .logo {
	font-weight: bold;
	float: left;
	background: <%= accent_color %>;
	
	margin-bottom: -1px;
	border-bottom: <%= border_width %>px solid <%= generic_border_color.to_s(:css_rgba) %>;
	border-right: <%= border_width %>px solid <%= generic_border_color.to_s(:css_rgba) %>;
	
	padding: 4px;
	padding-right: 6px;
	
	font-size: 16px;
	line-height: 16px;
}

#top-bar > .logo a {
	text-decoration: none;
	background: none;
	color: #fff;
}

#top-bar > .content {
	float: left;
	margin-left: 4px;
	position: relative;
	z-index: 1;
	padding: 2px;
	height: 20px;
	line-height: 20px;
}

#top-bar > .corner {
	float: right;
	margin-right: 4px;
	position: relative;
	z-index: 1;
	padding: 2px;
	height: 20px;
	line-height: 20px;
}



/* scope sidebar and list */

/* scope sidebar */
#scope-box {
	float: left; clear: left;
	width: 15em;
	margin: 1em;
}
/* root-level scope-list */
#scope-list > ul.scope-list {
	margin-left: 0;
	border: none;
	padding-left: 0.5em; /* 0em - scope-item's indent */
}

/* all nested scope-lists */
ul.scope-list {
	margin-left: -0.5em; /* 0em + scope-item's indent */
	border-left: 1px solid #eee;
	padding-left: 1.5em; /* 1em - scope-item's indent */
}
ul.scope-list > li.scope-item {
	list-style-type: none;
	margin-left: 0;
	text-indent: -0.5em;
}
/*.scope-list > .scope-item > .body { font-size: 1.0em; font-weight: bold; }
.scope-list > .scope-item > .body * { color: #666; }
.scope-list > .scope-item.current > .body { color: <%= accent_color %>; }*/


#sidebar-right {
	float: right; clear: right;
	position: relative; z-index: 1;
}


/* pile list */

#pile-box {
	float: left; clear: left;
	width: 15em;
	margin: 1em;
}

/*#pile-box > .pile { font-weight: bold; color: #666; }
#pile-box > .current.pile { color: <%= accent_color %>; }
#pile-box > .action {}*/


#view-options-box {
	float: left; clear: left;
	width: 15em;
	margin: 1em;
}

ul.pile-list { margin: 8px; }
ul.pile-list li .name { font-size: 15px; font-weight: bold; }
ul.pile-list li .line { font-size: 11px; }


/* user-bar */

#user-bar-greeting, #user-bar-action, #user-bar-greeting form { display: inline; }
