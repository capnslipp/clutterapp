<%- top_bar_back_shadow_height = 30 -%>
<%- top_bar_logo_graphic_img_dimens = {:width => 276, :height => 83} -%>
<%- top_bar_logo_margin = 20 -%>
.front #top-bar > .content {	margin-top: <%=top_bar_logo_margin%>px;	vertical-align: top;}
.front #top-bar > .content > .tagline {
	<%#position: absolute;
	left: <%=top_bar_logo_graphic_img_dimens[:width] + 16 %>px;
	top: <%=top_bar_logo_graphic_img_dimens[:height] - top_bar_logo_margin%>px;%>
		line-height: <%=top_bar_logo_graphic_img_dimens[:height]%>px;	
	font-size: 24px;
	color: #ddd;
	text-shadow: 0 2px 3px <%= shadow_color(0.5).to_s(:css_rgba) %>;
}

.front #top-bar > .back {
	/* override */ padding-bottom: <%=top_bar_back_shadow_height%>px;
}

.front #top-bar > .back > .fill {
	/* override */ /*background: <%= accent_color %>;*/
	/* override */ <%= %W{box-shadow -moz-box-shadow -webkit-box-shadow}.collect {|p| "#{p}: 0px 3px 18px #{shadow_color.to_s(:css_rgba)};" }.join(' ') %>
	
	margin-bottom: -<%=border_width*2%>px;
	border-bottom: <%=border_width*2%>px solid <%= generic_border_color.to_s(:css_rgba) %>;
	
	/* override */ bottom: <%=top_bar_back_shadow_height%>px;
}

.front #top-bar > .logo {
	/* override */ /*border: none;*/
	/* override */ /*background: none;*/
	
	padding: <%=top_bar_logo_margin%>px;
}

.front #top-bar > .logo > .graphic {
	float: left;
	
	width: 0;
	overflow: visible;
}


.front #top-bar > .logo > .graphic > img {	<%= top_bar_logo_graphic_img_dimens.collect {|p, d| "#{p}: #{d}px;" }.join(' ') %>		margin-left: -8px;	margin-bottom: -32px;}

.front #top-bar > .logo > .text {
	float: left;
	
	position: relative; z-index: 1;
	margin-left: 4px;
	
	font-size: 72px;
	line-height: 1em;
	
	color: transparent;
	/* IE */ filter: alpha(opacity=0);
}


.front #page {
	padding: 24px;
}


.front h3 { font-size: 32px; line-height: 1.25em; color: <%= text_color %>; }
.front h4 { font-size: 24px; line-height: 1.25em; color: <%= accent_color %>; }


.front #feature-box {
	display: inline-block;
	margin: auto 0;
}


.front #feature-box h4 { font-size: 18px; line-height: 1.25em; color: <%= wash_color %>; }

.front #feature-box ul {}
.front #feature-box li {}
.front #feature-box li.feature {}

.front #feature-box > img.screenshot {
	min-width: 512px;
	max-width: 100%;
}


.front h5 { font-size: 14px; line-height: 1.25em; color: <%= accent_color %>; }
.front h6 { font-size: 13px; line-height: 1.25em; color: <%= wash_color %>; }
