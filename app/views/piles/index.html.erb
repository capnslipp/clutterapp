<h1>Listing piles</h1>

<table>
  <tr>
  </tr>

<% @piles.each do |pile| %>
  <tr>
    <td><%= link_to 'Show', [@owner, pile] %></td>
    <td><%= link_to 'Edit', [:edit, @owner, pile] %></td>
    <td><%= link_to 'Destroy', [@owner, pile], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New pile', [:new, @owner, :pile] %>
